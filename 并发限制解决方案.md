# 🔍 并发限制问题诊断与解决方案

## 问题描述

**现象**：同时请求10个不同的text模型（不同session_id映射），只有6个模型在处理请求，剩余4个处于待机状态。

## 根本原因

### ✅ 已确认：Edge/Chrome浏览器的HTTP/1.1并发连接限制

**核心原因**：
- Edge和Chrome浏览器对**同一域名**限制为 **6个并发HTTP/1.1连接**
- 你的10个模型虽然使用不同的`session_id`，但它们都向同一个域名 `lmarena.ai` 发送请求
- 浏览器层面的限制导致只能同时处理6个请求，其余4个排队等待（Stalled状态）

**这不是你的代码问题**，而是浏览器的设计限制！

### 技术细节

根据[`api_server.py:2437-2468`](api_server.py:2437)的代码：
```python
# 使用线程安全的轮询策略选择映射
with MODEL_ROUND_ROBIN_LOCK:
    current_index = MODEL_ROUND_ROBIN_INDEX[model_name]
    selected_mapping = mapping_entry[current_index]
    # 每个请求获得不同的session_id
```

即使每个请求使用不同的session_id，但由于：
1. 所有请求都通过**同一个WebSocket连接**到油猴脚本
2. 油猴脚本在**同一个浏览器标签页**中运行
3. 该标签页对`lmarena.ai`的并发连接受浏览器限制

结果就是：**6个活跃 + 4个排队**

---

## 📊 验证方法

1. **使用诊断脚本**（已创建）：
   ```bash
   python debug_concurrent_diagnosis.py
   ```
   观察输出，如果看到"等待=4, 接收=6"则确认是浏览器限制。

2. **浏览器开发者工具验证**：
   - 按F12打开Edge开发者工具
   - 切换到 **Network** 标签
   - 同时发送10个请求
   - 观察：6个请求显示为`Pending/Transferring`，4个显示为`Stalled`

---

## ✅ 解决方案（按推荐顺序）

### 方案1：使用多个浏览器标签页 ⭐推荐⭐

**原理**：每个标签页有独立的6个连接配额，3个标签页 = 18个并发连接

**实现步骤**：

#### 步骤1：配置多标签页支持

修改 [`api_server.py:61-63`](api_server.py:61) 的WebSocket连接管理：

```python
# 当前代码（单连接）
browser_ws: WebSocket | None = None

# 修改为（支持多连接）
browser_connections: dict[str, WebSocket] = {}  # {tab_id: websocket}
connection_lock = asyncio.Lock()
```

#### 步骤2：修改WebSocket端点

更新 [`api_server.py:2093-2244`](api_server.py:2093) 的WebSocket处理：

```python
@app.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    global browser_connections
    await websocket.accept()
    
    # 接收标签页ID（由油猴脚本发送）
    init_message = await websocket.receive_text()
    init_data = json.loads(init_message)
    tab_id = init_data.get("tab_id", "default")
    
    async with connection_lock:
        browser_connections[tab_id] = websocket
        logger.info(f"✅ 标签页 {tab_id} 已连接。当前连接数: {len(browser_connections)}")
    
    # ... 其余逻辑保持不变
```

#### 步骤3：负载均衡请求到多个标签页

在 [`api_server.py:2330-2916`](api_server.py:2330) 的`chat_completions`中添加：

```python
# 选择负载最低的标签页
async def select_best_tab() -> str:
    """选择请求数最少的标签页"""
    if not browser_connections:
        raise HTTPException(503, "没有可用的浏览器连接")
    
    # 统计每个标签页的活跃请求数
    tab_loads = {}
    for tab_id in browser_connections.keys():
        tab_loads[tab_id] = sum(
            1 for req_id, metadata in request_metadata.items()
            if metadata.get("tab_id") == tab_id and req_id in response_channels
        )
    
    # 返回负载最低的标签页
    return min(tab_loads, key=tab_loads.get)

# 在发送请求时使用
selected_tab = await select_best_tab()
target_ws = browser_connections[selected_tab]
await target_ws.send_text(json.dumps(message_to_browser))
```

#### 步骤4：更新油猴脚本

在油猴脚本中添加：

```javascript
// 生成唯一的标签页ID
const TAB_ID = `tab_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;

// 连接时发送标签页ID
ws.onopen = () => {
    ws.send(JSON.stringify({ tab_id: TAB_ID }));
};
```

**优点**：
- ✅ 简单有效，3个标签页可支持18个并发
- ✅ 不依赖外部服务器配置
- ✅ 每个标签页独立，故障隔离

**缺点**：
- ⚠️ 需要手动打开多个LMArena标签页
- ⚠️ 代码改动较大

---

### 方案2：升级到HTTP/2（如果LMArena支持）

**原理**：HTTP/2支持多路复用，单个连接可处理无限并发请求

**检查LMArena是否支持HTTP/2**：
```bash
curl -I --http2 https://lmarena.ai
# 查看响应头中是否有 "HTTP/2"
```

**如果支持**，在油猴脚本的fetch请求中启用HTTP/2：
```javascript
// 现代浏览器会自动使用HTTP/2（如果服务器支持）
fetch('https://lmarena.ai/api/...', {
    // 无需特殊配置，浏览器自动协商
});
```

**优点**：
- ✅ 理论上无并发限制
- ✅ 无需代码改动

**缺点**：
- ❌ **LMArena可能不支持HTTP/2**（需验证）
- ❌ 你无法控制外部服务器

---

### 方案3：请求队列优化（治标方案）

如果无法增加并发连接，可以优化请求队列的处理效率。

在 [`api_server.py`](api_server.py) 中添加智能队列管理：

```python
class RequestQueue:
    """智能请求队列"""
    def __init__(self, max_concurrent=6):
        self.max_concurrent = max_concurrent
        self.semaphore = asyncio.Semaphore(max_concurrent)
        self.active_requests = set()
    
    async def execute(self, request_id, coro):
        """执行请求，自动限流"""
        async with self.semaphore:
            self.active_requests.add(request_id)
            try:
                result = await coro
                return result
            finally:
                self.active_requests.discard(request_id)

# 在chat_completions中使用
request_queue = RequestQueue(max_concurrent=6)
result = await request_queue.execute(request_id, handle_request())
```

**优点**：
- ✅ 服务器端可控，提升用户体验
- ✅ 避免请求堆积导致超时

**缺点**：
- ⚠️ 不解决根本问题（仍是6并发）
- ⚠️ 只是更优雅的排队

---

### 方案4：使用浏览器扩展替代油猴脚本（长期方案）

开发一个独立的浏览器扩展，利用`chrome.webRequest` API绕过标签页限制。

**优点**：
- ✅ 可配置独立的连接池
- ✅ 更好的性能

**缺点**：
- ❌ 开发成本高
- ❌ 需要用户安装扩展

---

## 🎯 推荐实施方案

### 短期方案（1-2天实现）

**方案1的简化版**：手动多标签页 + 轮询分配

1. **修改配置**，支持多WebSocket连接（见方案1步骤1-2）
2. **手动打开2-3个LMArena标签页**，每个运行油猴脚本
3. **后端自动轮询**分配请求到不同标签页

**预期效果**：
- 2个标签页 → 12并发
- 3个标签页 → 18并发（超过你的10个模型需求）

### 中期方案（1周内）

实施完整的**方案1**，包括：
- 自动标签页管理
- 智能负载均衡
- 故障转移

### 长期方案（未来）

如果LMArena升级支持HTTP/2，自动受益于无限并发。

---

## 📈 性能对比

| 方案 | 并发数 | 实现难度 | 推荐度 |
|------|--------|----------|--------|
| 当前（单标签页） | 6 | - | ⭐ |
| **多标签页（2个）** | **12** | **中** | **⭐⭐⭐⭐⭐** |
| 多标签页（3个） | 18 | 中 | ⭐⭐⭐⭐ |
| HTTP/2（如果支持） | ∞ | 低 | ⭐⭐⭐ |
| 请求队列优化 | 6 | 低 | ⭐⭐ |

---

## ⚙️ 快速验证

1. **立即验证浏览器限制**：
   ```bash
   # 打开Edge开发者工具（F12）→ Network标签
   # 同时发送10个请求
   # 观察是否有4个显示为"Stalled"
   ```

2. **测试多标签页方案**：
   ```bash
   # 手动打开2个LMArena标签页，都运行油猴脚本
   # 发送10个请求，观察是否都能立即处理
   ```

---

## 📝 总结

**问题确认**：✅ Edge浏览器HTTP/1.1限制（每域名6连接）

**最佳方案**：⭐ 使用2-3个浏览器标签页 + 后端负载均衡

**预期效果**：10个并发请求可以同时处理，无需等待

**实施时间**：1-2天（中等复杂度）

---

## 🔧 需要帮助？

如果需要我帮你实现方案1（多标签页支持），请告诉我，我可以：

1. 修改 `api_server.py` 支持多WebSocket连接
2. 实现智能负载均衡逻辑
3. 提供完整的测试方案

---

*生成时间：2025-01-05*  
*诊断工具：debug_concurrent_diagnosis.py*
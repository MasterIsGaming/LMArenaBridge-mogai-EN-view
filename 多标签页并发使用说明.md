# 🚀 多标签页并发使用说明

## 📋 功能概述

LMArena Bridge 现已支持**多标签页并发**，可以突破浏览器HTTP/1.1的6连接限制！

### 并发能力对比

| 标签页数量 | 理论最大并发 | 适用场景 |
|-----------|-------------|---------|
| 1个标签页 | 6个请求 | 轻度使用 |
| **2个标签页** | **12个请求** | **推荐配置** |
| 3个标签页 | 18个请求 | 重度使用 |

---

## 🔧 使用方法

### 步骤1：启动API服务器

```bash
python api_server.py
```

等待看到以下提示：
```
✅ 油猴脚本已成功连接 WebSocket。
📊 当前连接状态:
  - 活跃标签页数: 1
  - 理论最大并发: 6 个请求
```

### 步骤2：打开多个LMArena标签页

1. 在浏览器中打开 `https://lmarena.ai`
2. **复制标签页**（Ctrl+Shift+D 或右键菜单"复制标签页"）
3. 确保每个标签页都运行了油猴脚本

**验证方法**：查看服务器日志

```
✅ 标签页 'tab_1733389234567_a1b2c3' 已成功连接 WebSocket
📊 当前连接状态:
  - 活跃标签页数: 2
  - 理论最大并发: 12 个请求 (每标签页6个)
✅ 多标签页模式已激活！当前支持 12 个并发请求
```

### 步骤3：发送请求

正常使用！后端会**自动负载均衡**请求到不同标签页。

---

## 📊 负载均衡机制

### 智能分配策略

服务器会自动选择**负载最低**的标签页处理新请求：

```
请求1 → 标签页A (1/6)
请求2 → 标签页B (1/6)  # 自动选择空闲标签页
请求3 → 标签页A (2/6)
请求4 → 标签页B (2/6)
...
```

### 日志示例

```log
[LOAD_BALANCE] 选择标签页 'tab_xxx' (当前负载: 3/6)
[LOAD_BALANCE] 所有标签页负载: {'tab_xxx': 3, 'tab_yyy': 2}
```

---

## ⚠️ 注意事项

### 1. 标签页限制提示

**单标签页模式**时会看到警告：
```
⚠️  注意：单标签页模式，浏览器HTTP/1.1限制并发为6个请求
💡 如需更高并发，请打开额外的LMArena标签页并运行油猴脚本
   - 2个标签页 = 12并发
   - 3个标签页 = 18并发
```

### 2. 标签页不要关闭

保持所有LMArena标签页**打开**状态，关闭会减少并发能力。

### 3. 网络连接

确保所有标签页都能访问 `ws://localhost:5102/ws`

---

## 🐛 故障排查

### 问题：只有第一个标签页在工作

**原因**：其他标签页未运行油猴脚本

**解决**：
1. 刷新所有LMArena标签页
2. 检查油猴脚本是否启用
3. 查看浏览器控制台是否有错误

### 问题：仍然只有6个并发

**原因**：可能是请求计数未正确释放

**检查**：查看服务器日志中的 `[LOAD_BALANCE]` 信息

**解决**：重启API服务器

### 问题：标签页断开连接

**现象**：日志显示 `❌ 标签页 'xxx' 已断开连接`

**原因**：
- 标签页被关闭
- 浏览器休眠
- 网络断开

**解决**：刷新对应的标签页

---

## 📈 性能监控

### 实时查看并发状态

访问监控面板：`http://127.0.0.1:5102/monitor`

可以看到：
- 活跃标签页数量
- 每个标签页的负载
- 实时请求分配情况

### 日志关键字

搜索日志中的关键信息：

```bash
# 查看连接状态
grep "当前连接状态" logs/requests.jsonl

# 查看负载均衡
grep "LOAD_BALANCE" logs/requests.jsonl

# 查看释放计数
grep "已释放标签页" logs/requests.jsonl
```

---

## 🎯 最佳实践

### 推荐配置

**日常使用**：2个标签页（12并发）
- 足够应对大多数场景
- 资源占用适中

**重度使用**：3个标签页（18并发）
- 适合批量测试
- 多模型对比

### 性能优化

1. **保持标签页在前台**
   - 后台标签页可能降低性能
   - 建议使用窗口平铺模式

2. **定期刷新**
   - 长时间运行后可能需要刷新
   - 建议每2-3小时刷新一次

3. **监控内存**
   - 多标签页会增加内存占用
   - 关闭不必要的标签页

---

## 🔄 版本兼容性

### 支持的油猴脚本版本

- ✅ v2.7 及以上（推荐）
- ⚠️ v2.6 及以下（仅单标签页）

### 检查版本

查看油猴脚本控制台输出：

```
LMArena API Bridge v2.7 正在运行。
📋 标签页ID: tab_xxx
✅ 支持多标签页并发（绕过6连接限制）
```

---

## 📞 技术支持

如遇问题，请提供以下信息：

1. 服务器日志（最近50行）
2. 浏览器控制台输出
3. 标签页数量
4. 并发请求数

---

## 🎉 成功案例

**场景**：同时测试10个不同的text模型

**之前**（单标签页）：
```
6个模型立即响应 ✅
4个模型等待中 ⏳  <- 浏览器限制
```

**现在**（2个标签页）：
```
全部10个模型立即响应 ✅✅✅✅✅✅✅✅✅✅
负载均衡：标签页A(5个) + 标签页B(5个)
```

---

*最后更新：2025-01-05*  
*适用版本：LMArena Bridge v2.0+*